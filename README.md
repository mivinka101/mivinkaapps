# Lightfeed — минималистичный Reddit-клон

Проект демонстрирует базовый функционал Reddit в современном стеке **Next.js 14 (App Router) + TypeScript + Tailwind CSS + Prisma (SQLite)**.

## Возможности

- 📚 Лента последних постов с карточным UI и превью изображений.
- 🗂️ Страницы сабреддитов с описанием и обсуждениями.
- ✍️ Создание постов с выбором сабреддита и опциональным изображением.
- 👍/👎 Голосование за посты с оптимистичными обновлениями.
- 💬 Вложенные комментарии с формами ответа.
- 🔐 Авторизация через GitHub (NextAuth + Prisma Adapter).

## Структура

```
.
├── app
│   ├── actions               # server actions для постов и комментариев
│   ├── api/auth/[...nextauth]# NextAuth handler
│   ├── create-post           # страница создания поста
│   ├── (auth)/signin         # страница входа
│   ├── subreddit/[slug]      # страницы сабреддитов
│   ├── layout.tsx            # общий layout с Inter и SessionProvider
│   └── page.tsx              # главная лента
├── components                # UI-компоненты, карточки постов, комментарии
├── lib                       # Prisma client, NextAuth options, утилиты
├── prisma
│   └── schema.prisma         # модели пользователей, постов, комментариев, голосов
├── styles
│   └── globals.css           # Tailwind слой и глобальные стили
├── .env.example              # переменные окружения
└── README.md
```

## Подготовка окружения

1. Скопируйте `.env.example` в `.env` и заполните значения GitHub OAuth приложения:

   ```bash
   cp .env.example .env
   ```

   - `GITHUB_ID` и `GITHUB_SECRET` — данные вашего OAuth app.
   - `NEXTAUTH_SECRET` — любая случайная строка (можно сгенерировать `openssl rand -base64 32`).

2. Установите зависимости:

   ```bash
   npm install
   ```

3. Примените миграции и заполните базу демо-данными:

   ```bash
   npm run prisma:migrate
   npm run prisma:seed
   ```

   Скрипт `prisma:seed` создаст несколько сабреддитов, постов, голосов и комментариев, чтобы лента была наполнена сразу после миграций.

4. (Необязательно) Откройте Prisma Studio для просмотра и редактирования данных:

   ```bash
   npx prisma studio
   ```

5. Запустите приложение в режиме разработки:

   ```bash
   npm run dev
   ```

   Интерфейс будет доступен на http://localhost:3000.

## Скрипты npm

- `npm run dev` — Next.js dev server.
- `npm run build` — production-сборка.
- `npm run start` — запуск собранного приложения.
- `npm run lint` — проверка ESLint.
- `npm run postinstall` — генерация Prisma Client (запускается автоматически).
- `npm run prisma:migrate` — применение миграций базы данных в режиме разработки.
- `npm run prisma:seed` — заполнение базы демонстрационными данными через Prisma.

## Дополнительно

- Все стили построены на Tailwind с кастомной палитрой и шрифтом Inter.
- Авторизация реализована через NextAuth c GitHub-провайдером и Prisma Adapter.
- UI подчёркивает карточную сетку, круглые аватары и акцент на изображениях.
